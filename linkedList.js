const LinkedList = function() {
    const head = {
        next: null
    }

    return { head };
};

const Node = function(value) {
    value: value,
    next: null;
};

module.exports = { LinkedList };